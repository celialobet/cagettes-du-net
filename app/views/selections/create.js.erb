
<% if @selection.basket_id == nil %>
  var titleAdditionalProduct = "<%= @additional_product.title %>"
  alert("Le produit (" + titleAdditionalProduct.toLowerCase() + ") a été ajouté au panier");

<% elsif @selection.additional_product_id == nil %>
  var titleBasket = "<%= @basket.title %>"
  alert("La " + titleBasket.toLowerCase() + " a été ajoutée");
<% end %>