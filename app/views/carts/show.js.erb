var additionalProduct = <%= @cart.additional_product.length %>;

if (additionalProduct == 0) {
  document.getElementById("order-area").innerHTML ="<%= j render 'cart_basket'%>";
}

else {
  document.getElementById("order-area").innerHTML ="<%= j render 'cart_additional_product'%>";
}